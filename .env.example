# Next.js app (server-side reads)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Ask feature — Cohere SQL generation + NL summaries
COHERE_API_KEY=

# Ask feature — read-only Postgres role (transaction-mode pool URL)
# Run scripts/ask_readonly_setup.sql in Supabase first.
ASK_READONLY_DATABASE_URL=

# Scraper only — never expose to browser or Vercel frontend
SUPABASE_SERVICE_ROLE_KEY=
